include: package:flutter_lints/flutter.yaml

analyzer:
  # Exclude generated files from all analysis.
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"

  errors:
    invalid_annotation_target: ignore

  # Language settings control strictness
  language:
    strict-raw-types: true

  # Enable full strong-mode for maximum type safety.
  strong-mode:
    implicit-casts: false
    implicit-dynamic: false

# You can customize the analysis rules here.
linter:
  rules:
    # --- Format/quotes ---
    prefer_single_quotes: true

    # --- UI/Widgets ---
    avoid_unnecessary_containers: true
    sized_box_for_whitespace: true
    use_colored_box: true
    use_decorated_box: true
    sort_child_properties_last: true

    # --- Immutability/performance ---
    prefer_const_literals_to_create_immutables: true
    prefer_final_in_for_each: true

    # --- Async/resources ---
    avoid_void_async: true
    cancel_subscriptions: true
    close_sinks: true

    # --- Null-safety / logic ---
    avoid_returning_null_for_future: true
    literal_only_boolean_expressions: true
    avoid_bool_literals_in_conditional_expressions: true

    # --- Imports/order ---
    always_use_package_imports: true
    avoid_relative_lib_imports: true
    directives_ordering: true

    # --- Constants ---
    prefer_const_constructors: true
    prefer_const_declarations: true
    prefer_const_constructors_in_immutables: true

    # --- Finals ---
    prefer_final_fields: true
    prefer_final_locals: true

    # --- Style and readability ---
    prefer_is_empty: true
    prefer_collection_literals: true
    avoid_redundant_argument_values: true
    avoid_print: true
    avoid_empty_else: true
    curly_braces_in_flow_control_structures: true
    require_trailing_commas: true

    # --- Unnecessary code ---
    unnecessary_breaks: true
    unnecessary_parenthesis: true
    unnecessary_const: true
    unnecessary_this: true
    unnecessary_lambdas: true
    unnecessary_overrides: true

    # --- Safety and async handling ---
    use_build_context_synchronously: true
    unawaited_futures: true
    parameter_assignments: true
    always_declare_return_types: true

    # --- Dependency and package rules ---
    depend_on_referenced_packages: true
