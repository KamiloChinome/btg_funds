import 'package:flutter/material.dart';

/// BTG Pactual brand color palette.
abstract final class AppColors {
  /// Primary dark navy blue.
  static const primary = Color(0xFF0D2137);

  /// Lighter navy for gradients and accents.
  static const primaryLight = Color(0xFF1B4F72);

  /// Gold accent color.
  static const accent = Color(0xFFC8A951);

  /// FPV category color — blue.
  static const fpvCategory = Color(0xFF1565C0);

  /// FIC category color — teal.
  static const ficCategory = Color(0xFF00897B);

  /// Success green.
  static const success = Color(0xFF2E7D32);

  /// Error red.
  static const error = Color(0xFFC62828);

  /// Subtle background for cards.
  static const surfaceLight = Color(0xFFF5F7FA);
}
